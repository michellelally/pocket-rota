<div class="container m-auto my-4">
  <% unless current_user.employees.any? %>
    <%= render "employees_cta" %>
  <% else %>
    <%= render "employees_landing",  employees: @employees %>
  <% end %>
</div 